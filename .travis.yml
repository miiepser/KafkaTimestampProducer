git:
  submodules: true


branches:
  only:
  - master
  - stable

language: java
install: true

sudo: required

services:
  - docker

addons:
  apt:
    packages:
    - docker-ce

script:
  - docker login -u "$DOCKER_USER" -p "$DOCKER_PASS"
  - ./gradlew jib